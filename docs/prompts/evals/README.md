# Prompt evals

5 golden cases per pipeline. Each is a YAML file with input, fixture,
and assertions. See PLAN.md §11.3 for the schema and CI policy
(mock-based evals run on every PR; real-API evals run as a manual CI job).

Lands in `feat/prompt-evals` (Week 8).
