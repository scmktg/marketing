# Architecture Decision Records

One markdown file per decision. Format: short context, options
considered, decision, consequences.

The implementation plan (`docs/PLAN.md`) covers most decisions; ADRs
are for things that arise mid-build and don't fit cleanly into the plan.
